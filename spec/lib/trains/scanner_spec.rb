# frozen_string_literal: true

describe Trains::Scanner do
  let(:dir) { "fixtures/barebones_app" }

  context "Given a valid Rails directory" do
    it "generates a list of all controllers in the repo" do
    end
  end
end
